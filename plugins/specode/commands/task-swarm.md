---
description: 多 agent 并发任务执行（v0.7+ 可用；v0.6 占位）
argument-hint: "[<spec-dir>/tasks.md] [--max-parallel N] [--max-rounds N]"
---

> ⚠️ **v0.6 不可用**。task-swarm 多 agent 编排器将在 v0.7 引入；当前版本若收到 `/specode:task-swarm`，请回复：
>
> > task-swarm 模式将在 v0.7 引入，当前 v0.6 请用 `/specode:spec` 顺序执行 tasks。

/specode:task-swarm $ARGUMENTS
